
TODO dropsi:

- remove rpettyprint for dates and use #temporal.format in thymeleaf
- rename editInfo to just info

- make scrollbars own stylesheet and add as needed

-

- add some color
- add a loading modal

- make AccessLogEntries texts constants somewhere

- rename main to something more telling
- make modals in main a fragment

- add Transactional methods where needed even if its the same as default
- when a new folder is built check if a folder with same name exists and if yes rename to name(x)
- when a file with same name is uploaded rename to name(x)

- refactor sign up to register
